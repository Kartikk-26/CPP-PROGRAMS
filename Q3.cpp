/*#include <iostream>
#include <cmath>

class Triangle {
public:
    double side1;
    double side2;
    double side3;

    // Constructor to initialize the sides and calculate/print area and perimeter
    Triangle(double s1, double s2, double s3) {
        side1 = s1;
        side2 = s2;
        side3 = s3;

        double s = (side1 + side2 + side3) / 2.0;
        double area = sqrt(s * (s - side1) * (s - side2) * (s - side3));
        double perimeter = side1 + side2 + side3;

        std::cout << "Triangle with sides: " << side1 << ", " << side2 << ", " << side3 << std::endl;
        std::cout << "Area of the triangle: " << area << " square units" << std::endl;
        std::cout << "Perimeter of the triangle: " << perimeter << " units" << std::endl;
    }
};

int main() {
    // Create a Triangle object with sides 3, 4, and 5 units
    Triangle triangle(3.0, 4.0, 5.0);

    return 0;
}*/

#include<iostream>
